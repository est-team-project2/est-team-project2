spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/project
    username: Hwan
    password: 0000!
  config:
    activate:
      on-profile: local

  jpa:
    hibernate:
      ddl-auto: none

